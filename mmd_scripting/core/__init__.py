"""
These functions are used extremely frequently and make up the core of the package.
contents:
    PMX struct definitions
    PMX reader/writer
    VMD struct definitions
    VMD reader/writer
    VPD reader/writer (no dedicated struct, internally represented as a VMD)
    core file input/output, user input/output, math stuff, etc etc etc
"""